# ChatBot

## Local development

### Setup

```bash
$ cp .env.example .env
$ docker-compose up -d
$ npm run start:dev
```

### Swagger
Swagger is available on your [localhost:${APP_PORT}/swagger](http://localhost:3050/swagger)